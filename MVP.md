# MiniK8s MVP - 最基础可用版本

## 功能范围

### 核心功能（必须实现）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| **Pod 管理** | 创建、查看、删除 Pod | P0 |
| **多节点支持** | 注册节点、节点心跳、资源上报 | P0 |
| **基础调度** | 将 Pod 调度到可用节点 | P0 |
| **容器运行时** | 通过 Docker 运行容器 | P0 |
| **跨平台** | Linux/macOS/Windows 同时支持 | P0 |

### 不支持的功能（MVP 阶段）

- Service/网络（使用 host 网络）
- 健康检查/重启策略
- ConfigMap/Secret
- 持久化存储
- 资源限制
- Watch 机制

---

## 跨平台方案

### 统一使用 Docker 作为运行时

所有平台都通过 Docker API 管理容器，确保行为一致性：

```
┌─────────────────────────────────────────────────────────────┐
│                    MiniK8s Control Plane                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  API Server │  │  Scheduler  │  │  Node Manager       │  │
│  │  (HTTP)     │  │  (简单调度)  │  │  (节点管理)          │  │
│  └──────┬──────┘  └─────────────┘  └─────────────────────┘  │
│         │                                                    │
│         │ SQLite (单文件存储)                                 │
│         ▼                                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Docker API (跨平台统一)                    │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   ┌─────────┐          ┌─────────┐          ┌─────────┐
   │  Node 1 │          │  Node 2 │          │  Node N │
   │ (Docker)│          │ (Docker)│          │ (Docker)│
   └─────────┘          └─────────┘          └─────────┘
```

### 平台适配

| 平台 | Docker 安装方式 | 网络模式 |
|------|----------------|----------|
| Linux | 原生 Docker | bridge |
| macOS | Docker Desktop | host (端口映射) |
| Windows | Docker Desktop/WSL2 | host (端口映射) |

---

## 架构简化

### 单二进制设计

```
minik8s 二进制包含：
├── server 模式: API Server + Scheduler + Node Manager
└── agent 模式: 节点代理 (在每个工作节点运行)
```

### 存储简化

使用 SQLite 单文件存储：
- 资源定义 (pods, nodes)
- 状态信息
- 调度结果

### 网络简化

MVP 阶段使用 **host 网络模式**：
- 容器直接使用宿主机网络
- 通过端口映射避免冲突
- 不涉及 CNI 复杂性

---

## 快速开始

### 1. 安装 Docker

```bash
# Linux
curl -fsSL https://get.docker.com | sh

# macOS/Windows
# 下载 Docker Desktop
```

### 2. 启动 Server

```bash
# 主节点
minik8s server --bind 0.0.0.0:8080
```

### 3. 添加工作节点

```bash
# 在其他机器上
minik8s agent --server http://<master-ip>:8080 --name node-1
```

### 4. 创建 Pod

```bash
kubectl create -f pod.yaml
kubectl get pods
kubectl get nodes
```

---

## 实现计划

1. **项目脚手架** - 1 天
2. **API Server + SQLite** - 2 天
3. **Docker 运行时** - 2 天
4. **Pod 生命周期** - 2 天
5. **多节点支持** - 2 天
6. **基础调度** - 1 天
7. **CLI 工具** - 1 天
8. **跨平台测试** - 1 天

**总计：约 2 周**
